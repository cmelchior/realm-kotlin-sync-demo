pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Realm_Kotlin_Sync_Demo"
include(":androidApp")
include(":shared")